# Pinned runtime dependencies. Dev/test extras live in pyproject.toml ([dev]).
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
SQLAlchemy>=2.0,<2.1
alembic>=1.13,<2.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
# PostgreSQL driver, used when DATABASE_URL points at Postgres (see docker-compose).
psycopg[binary]>=3.1,<4.0
