# SQLAlchemy database URL.
# Local default uses SQLite; point this at Postgres in production, e.g.
#   postgresql+psycopg://stock:stock@localhost:5432/stockroom
DATABASE_URL=sqlite:///./stockroom.db

# API key required on write endpoints (POST/PUT/DELETE). Leave empty to disable.
API_KEY=local-dev-key
